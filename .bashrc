# Ubuntu
source ~/.bash_profile

export PATH=/usr/lib/postgresql/9.3/bin:$PATH

# pbcopy and pbpaste for Linux
alias pbcopy='xsel --clipboard --input'
alias pbpaste='xsel --clipboard --output'

# debchange
export DEBFULLNAME="Stefan Sundin"
export DEBEMAIL=stefan@example.com

# if you prefer Ubuntu to open new terminal tabs in home directory instead of keeping same working directory
cd ~
