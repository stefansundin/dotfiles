# comment out GRUB_HIDDEN_TIMEOUT to make grub menu visible

# skip graphical logo
GRUB_CMDLINE_LINUX_DEFAULT="text"

# set graphics options
GRUB_CMDLINE_LINUX="vga=792 consoleblank=0"
GRUB_GFXMODE="1024x768"

# increase scrollback buffer
GRUB_CMDLINE_LINUX="fbcon=scrollback:1024k"

# use legacy eth0 and wlan0 names for network interfaces
GRUB_CMDLINE_LINUX="net.ifnames=0 biosdevname=0"

# don't forget "sudo update-grub"
